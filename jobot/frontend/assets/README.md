/**
 * Placeholder per la cartella assets del frontend Expo.
 * 
 * TODO: sostituire con le icone e immagini reali prima della pubblicazione su store.
 * Per la demo, Expo usa icone di default se i file non esistono.
 */
